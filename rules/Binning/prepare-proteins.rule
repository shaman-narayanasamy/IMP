rule prepare_proteins:
    #IN: <directory w/ unprepared .faa files> <OUT_DIR>
    input:
       "%s/separate_bins.done" % B_OUT
    output:
       "%s/prepare_proteins.done" % B_OUT
    shell:
        """
        src/prepareProteins.sh {B_OUT}/clusterFiles {B_OUT}/clusterFiles/preparedproteins
        touch {output}
        """

