rule binning_prepare_proteins:
    #IN: <directory w/ unprepared .faa files> <OUT_DIR>
    input:
        "Binning/clusterFiles/cluster.{cluster}.faa"
    output:
        "Binning/clusterFiles/cluster.{cluster}_prepared.faa"
    shell:
        """
        cp {input} {output}
        """

