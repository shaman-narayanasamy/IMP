# include rules for the Binning part of the workflow

include:
    '../../rules/Binning/find-essential-genes.rule'

include:
    '../../rules/Binning/binning.rule'

include:
    '../../rules/Binning/separate-bins.rule'

include:
    '../../rules/Binning/prepare-proteins.rule'

include:
    '../../rules/Binning/checkm.rule'

# master command
rule BINNING:
    input:
        #"Binning/clusterFiles/reachabilityDistanceEstimates.tsv"
        "Binning/clusterFiles/N.gff"
        #Binning/clusterFiles/preparedproteins/N.faa
    output:
        touch('binning.done')
