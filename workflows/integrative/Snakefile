# include configuration file
include:
    "../../rules/config"

# define the data types and map the data input to variables
TYPES = ['mg', 'mt']


#include:
#    "../../rules/databases.rules"

workdir:
    OUTPUTDIR

# include rules for the workflow
include:
    "Preprocessing"

# include rules for the workflow
include:
    "Assembly"

# master command
rule ALL:
    input:
        "preprocessing.done",
        "assembly.done"
    output:
        touch('workflow.done')
